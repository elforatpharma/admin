# نظام كوبونات الخصم - الفرات فارما

## ✅ ما تم إضافته

### 1. في لوحة التحكم (`admin.html`)

#### أ. قائمة جانبية جديدة
- رابط "الكوبونات" في القائمة الجانبية مع أيقونة `local_offer`

#### ب. قسم إدارة الكوبونات
- جدول عرض الكوبونات مع الأعمدة التالية:
  - كود الكوبون
  - نسبة الخصم
  - تاريخ الانتهاء
  - الحالة (نشط/متوقف/منتهي)
  - إجراءات (تفعيل/إيقاف/حذف)
- الكوبون الذي انتهى تاريخه يُعرض تلقائياً كـ "منتهي" ولا يظهر له زر تفعيل.

#### ج. مودال إضافة كوبون
- حقل كود الكوبون (يُحوّل تلقائياً لأحرف كبيرة)
- نسبة الخصم (1-99%)
- تاريخ الانتهاء (منتقي تواريخ، يبدأ من اليوم افتراضياً)
- وصف اختياري
- رسالة واضحة عند تكرار كود موجود (اختيار كود مختلف)

#### د. الوظائف البرمجية
```javascript
- showCouponsSection()       // التنقل لقسم الكوبونات
- fetchCoupons()             // جلب وعرض جميع الكوبونات
- toggleCoupon(id, state)    // تفعيل/إيقاف كوبون
- deleteCoupon(id)           // حذف كوبون
- openCouponModal()          // فتح مودال الإضافة
- closeCouponModal()         // إغلاق المودال
- initCoupons()              // تهيئة النظام ومعالجة النموذج
```

### 2. ملف قاعدة البيانات (`coupons_schema.sql`)

#### جدول `coupons`
| العمود | النوع | الوصف |
|--------|-------|--------|
| id | UUID | المعرف الفريد |
| code | TEXT | كود الكوبون (فريد) |
| discount_percentage | INTEGER | نسبة الخصم (1-99) |
| expiry_date | DATE | تاريخ الانتهاء |
| description | TEXT | وصف الكوبون |
| is_active | BOOLEAN | الحالة (نشط/متوقف) |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| updated_at | TIMESTAMP | تاريخ آخر تحديث |

#### فهرسة متقدمة
- فهرس على `code` للبحث السريع
- فهرس مركب على `is_active` و `expiry_date`

#### سياسات الأمان (RLS)
- القراءة متاحة للجميع (للمتجر وللآدمن)
- الكتابة والتعديل والحذف للمستخدمين المصادقين فقط
- انظر ملف `rls_policies.sql` لبقية الجداول

---

## 📋 خطوات التثبيت

1. افتح **Supabase Dashboard** → **SQL Editor** ثم انسخ والصق محتوى ملف `coupons_schema.sql` واضغط **Run**.
2. تأكد من ظهور جدول `coupons` في **Table Editor**.
3. شغّل أيضاً `rls_policies.sql` لتطبيق سياسات الأمان على كل الجداول.

---

## 🎯 كيفية الاستخدام

1. اضغط على زر "الكوبونات" في القائمة الجانبية.
2. اضغط "إضافة كوبون" واملأ البيانات مثل: الكود `WELCOME20`، الخصم `20`، تاريخ النهاية، ووصف اختياري.
3. اضغط "حفظ الكوبون".
4. للإدارة: زر إيقاف/تفعيل وزر حذف.

---

## 🔌 التكامل مع صفحة الدفع (Checkout)

في متجر العملاء:

```javascript
async function applyCoupon(couponCode) {
  const { data, error } = await _supabase
    .from('coupons')
    .select('*')
    .eq('code', couponCode.toUpperCase().trim())
    .eq('is_active', true)
    .gte('expiry_date', new Date().toISOString().split('T')[0])
    .maybeSingle();

  if (error || !data) {
    return { success: false, message: 'كود الكوبون غير صالح' };
  }

  const discountAmount = (total * data.discount_percentage) / 100;
  const finalTotal = total - discountAmount;

  return { success: true, discount: discountAmount, finalTotal, couponCode: data.code };
}
```

---

## 📝 ملاحظات هامة

1. **الكود فريد**: لا يمكن إضافة كوبونين بنفس الكود.
2. **الأحرف الكبيرة**: يتم تحويل الكود تلقائياً لأحرف كبيرة.
3. **التاريخ**: الكوبون يُعتبر منتهياً مقارنة بتاريخ اليوم.
4. **الأمان**: فقط المستخدم المسجل (مصادق عليه) يمكنه إدارة الكوبونات.

---

**تم التطوير بواسطة فريق الفرات فارما 💖**